  stages:
    - deploy

  pages:
    stage: deploy
    script:
      - mkdir -p public
      - cp index.html style.css public/
      - sed -i "s/%%VERSION%%/$CI_COMMIT_SHORT_SHA/g" ./public/index.html
    artifacts:
      paths:
        - public
    only:
      - master
